import { ArrangedatePipe } from './arrangedate.pipe';

describe('ArrangedatePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrangedatePipe();
    expect(pipe).toBeTruthy();
  });
});
